{
  "name": "Nabhold equator-estate",
  "image": "ghcr.io/nabhold/baobab-dev:1.2.6",
  "remoteUser": "vscode",
  "updateRemoteUserUID": true,
  "init": true,
  "postCreateCommand": "baobab-verify --contract && if [ ! -f .env ]; then cp .env.example .env; fi && pip install -r requirements.txt && python manage.py check",
  "customizations": {
    "vscode": {
      "extensions": ["github.vscode-github-actions", "redhat.vscode-yaml"]
    }
  }
}
